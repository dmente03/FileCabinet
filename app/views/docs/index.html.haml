- @docs.each do |doc|
	%h2= link_to doc.title, doc
	%p= time_ago_in_words(doc.created_at)
	%p= truncate(doc.content, length: 50)
	= link_to "Edit", edit_doc_path(doc)
	= link_to "Delete", doc_path(doc), method: :delete, data: { confirm: "Are you sure?" }

= link_to "Create", new_doc_path